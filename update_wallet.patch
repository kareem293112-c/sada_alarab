--- src/components/profile/WalletView.tsx
+++ src/components/profile/WalletView.tsx
@@ -5,7 +5,7 @@
 import { doc, updateDoc, getDoc, addDoc, collection } from 'firebase/firestore';
 
@@ -42,7 +42,7 @@
   );
 
-  const handleConvert = async (e: React.FormEvent) => {
+  const handleCashout = async (e: React.FormEvent) => {
     e.preventDefault();
     setErrorMessage('');
     setSuccessMessage('');
@@ -78,21 +78,26 @@
         }
 
         const newDiamonds = currentDiamonds - amount;
-        const newCoins = currentCoins + Math.floor(amount / 2); // 50% conversion ratio: 2 diamonds = 1 coin
+        
+        // Cashout math: 100,000 Diamonds = $100 total value. 
+        // User gets 80% ($80), platform gets 20% ($20).
+        const withdrawalValueUSD = (amount / 100000) * 80.00;
+        const platformRevenueUSD = (amount / 100000) * 20.00;
 
         // Update Firestore persistently
         await updateDoc(userRef, {
           diamonds: newDiamonds,
-          coins: newCoins
         });
+        
+        // Record withdrawal request and platform revenue
+        await addDoc(collection(db, "cashout_requests"), {
+          userId: currentUser.id,
+          diamonds_deduched: amount,
+          withdrawal_usd: withdrawalValueUSD,
+          platform_revenue_usd: platformRevenueUSD,
+          status: 'pending',
+          created_at: new Date().toISOString()
+        });
 
         // Update local visual states
         setLiveDiamonds(newDiamonds);
-        setLiveCoins(newCoins);
         setConvertAmount('');
-        setSuccessMessage(`🎉 تم تحويل ${amount.toLocaleString()} ماسة بنجاح إلى ${Math.floor(amount / 2).toLocaleString()} كوينز ذهبي!`);
+        setSuccessMessage(`🎉 تم تقديم طلب سحب بقيمة $${withdrawalValueUSD.toFixed(2)} USD بنجاح!`);
         
         // Auto clear success banner
         setTimeout(() => setSuccessMessage(''), 6000);
@@ -237,27 +242,27 @@
                 <div className="flex items-center gap-1.5 text-right">
                   <TrendingUp className="w-4 h-4 text-emerald-400 shrink-0" />
-                  <span className="text-[10px] font-black text-slate-300">معدل التحويل الحقيقي</span>
+                  <span className="text-[10px] font-black text-slate-300">نسبة أرباح المنصة</span>
                 </div>
                 <span className="text-[10px] font-black text-amber-300 bg-amber-500/10 border border-amber-500/20 px-2.5 py-0.5 rounded-full">
-                  2 ماسة 💎 = 1 كوينز 🪙 (نسبة 50%)
+                  100,000 💎 = $80.00 USD (نسبة سحب 80%)
                 </span>
               </div>
             </div>
 
-            {/* Live Interactive Conversion Form */}
+            {/* Live Interactive Cashout Form */}
             <div className="bg-[#121118] border border-white/5 rounded-3xl p-5 space-y-5">
               <div className="flex items-center justify-between border-b border-white/5 pb-3">
                 <div className="flex items-center gap-2">
                   <ArrowLeftRight className="w-5 h-5 text-pink-400" />
-                  <h4 className="text-sm font-black text-slate-200">تحويل الألماس إلى كوينز ذهبي</h4>
+                  <h4 className="text-sm font-black text-slate-200">سحب الأرباح (Cashout)</h4>
                 </div>
-                <span className="text-[10px] text-slate-400 font-semibold">تحويل داخلي فوري فخم</span>
+                <span className="text-[10px] text-slate-400 font-semibold">سحب مالي حقيقي</span>
               </div>
 
-              <form onSubmit={handleConvert} className="space-y-4">
+              <form onSubmit={handleCashout} className="space-y-4">
                 <div className="space-y-2">
-                  <label className="text-xs font-black text-slate-400 block text-right pr-1">الكمية المراد تحويلها:</label>
+                  <label className="text-xs font-black text-slate-400 block text-right pr-1">الكمية المراد سحبها:</label>
                   <div className="relative">
                     <input
@@ -275,12 +280,12 @@
                 </div>
 
-                {/* Conversion Preview */}
+                {/* Cashout Preview */}
                 {parseInt(convertAmount) > 0 && (
                   <div className="bg-slate-500/5 p-4 rounded-xl border border-dashed border-white/5 flex items-center justify-between text-sm animate-fade-in">
-                    <span className="text-xs text-slate-400 font-bold">سوف تستلم فورا (نسبة 50%):</span>
-                    <div className="flex items-center gap-1.5 font-mono font-black text-amber-300 text-base">
-                      <span>🪙</span>
-                      <span>{Math.floor(parseInt(convertAmount) / 2).toLocaleString()} كوينز</span>
+                    <span className="text-xs text-slate-400 font-bold">قيمة السحب المستحقة (80%):</span>
+                    <div className="flex items-center gap-1.5 font-mono font-black text-emerald-400 text-base">
+                      <span>$</span>
+                      <span>{((parseInt(convertAmount) / 100000) * 80).toFixed(2)} USD</span>
                     </div>
                   </div>
                 )}
@@ -291,10 +296,10 @@
                 >
                   {isConverting ? (
-                    <span>جاري التحويل الآمن...</span>
+                    <span>جاري تقديم الطلب...</span>
                   ) : (
                     <>
-                      <span>✨ إتمام عملية التحويل الفوري</span>
+                      <span>✨ تأكيد عملية السحب</span>
                     </>
                   )}
                 </button>
